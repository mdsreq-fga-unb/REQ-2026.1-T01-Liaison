# Design Context — Liaison

> **Last Updated:** 2026-06-28 | **Figma:** `f6bQuVohTvZLF5WWPEbNob`
> **Design System:** Custom (tokens em `frontend/src/theme/`)

---

## 1. Design System Overview

Mobile-first (Expo / React Native), baseline 390px de largura. Estilo via `StyleSheet` nativo + tokens centralizados — **sem NativeWind**. Linguagem visual: navy + dourado sobre fundo creme, tipografia editorial (Playfair Display) com corpo neutro (DM Sans). Uso de tokens é **obrigatório**; cores/spacing hardcoded são bug.

---

## 2. Color Palette

| Token | Value | Usage |
|-------|-------|-------|
| navy | `#1a2744` | Títulos, ações primárias |
| gold | `#d4813a` | Acento, CTAs secundárias |
| background (cream) | `#faf8f4` | Fundo de tela |
| border | `#ddd8ce` | Divisores, contornos |
| error | `#ef4444` | Erros |

> Fonte: `frontend/src/theme/colors.ts` (token exato lá).

---

## 3. Typography

- **Títulos:** Playfair Display
- **Corpo:** DM Sans
- Carregadas via `@expo-google-fonts`. Definições em `frontend/src/theme/typography.ts`.

---

## 4. Spacing & Layout

- **Baseline:** 390px (mobile-first).
- Tokens de spacing em `frontend/src/theme/spacing.ts`.
- Sem breakpoints web (app nativo). Header com `headerShown:false` precisa `paddingTop:50` (status bar).

---

## 5. Component Patterns

- Componentes reutilizáveis em `frontend/src/components/ui/`, `auth/`, `profile/`.
- 29 SVGs custom + Ionicons via `react-native-svg-transformer`.
- Componentes `PascalCase.tsx`, testes co-localizados em `__tests__/`.

---

## 6. Assets

- **Ícones:** Ionicons + 29 SVGs custom (`assets/`, importados como componentes via svg-transformer).
- **Fontes:** Google Fonts via Expo.

---

## 7. Figma File Map

| Page | Purpose | Node IDs |
|------|---------|----------|
| _A definir_ | fileKey `f6bQuVohTvZLF5WWPEbNob` | _A definir_ |
| Avaliação de Candidaturas (#24, RF11) — redesenho on-system + ícones | Dashboard candidatos / Gestão (abas) / Avaliar candidato / Aprovados / Modal reverter | `774:2`, `775:2`, `785:2`, `777:2`, `778:2` |

---
*Generated by context-generator on 2026-06-28*
